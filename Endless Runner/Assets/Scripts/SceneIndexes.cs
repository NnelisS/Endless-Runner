using System.Collections;
using UnityEngine;

//indexes the scene changer uses for loading screen
public enum SceneIndexes
{
    MANAGER = 0,
    TITLE_SCREEN = 1,
    MAP = 2,
    ENDCUTSCENE = 3,
} 
